/* import React from 'react'

function SumArray(props) {
    let myArr=props.passArr;
    let sumNum=0;
    for (let i = 0; i < myArr.length; i++) {
        if(typeof(myArr)===Number){
            sumNum+=myArr[i];
        }else{
            console.log("not a number Array");
        }
    }
    console.log(sumNum)
  return (
    <div>

    </div>
  )
}

export default SumArray */